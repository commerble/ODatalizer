name: test EF6

on: 
  push:
    branches: 
      - '**'
    tags-ignore:
      - '*.*'
    paths:
      - 'src/ODatalizer/**'
      - 'src/ODatalizer.EF6/**'
      - 'src/ODatalizer.EF6.Tests/**'
      - 'src/ODatalizer.EF6.sln'
      - '.github/workflows/test.yml'
  pull_request:
    branches: 
      - '**'
    tags-ignore:
      - '*.*'
    paths:
      - 'src/ODatalizer/**'
      - 'src/ODatalizer.EF6/**'
      - 'src/ODatalizer.EF6.Tests/**'
      - 'src/ODatalizer.EF6.sln'
      - '.github/workflows/test.yml'

jobs:
  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.x
    - name: Run all tests
      working-directory: ./src/
      run: dotnet test ODatalizer.EF6.sln
